<UserControl
    x:Class="Swoa.UI.CelestialObject"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Swoa.UI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="20"
    d:DesignWidth="20"
    mc:Ignorable="d">

    <!--<UserControl.Resources>
        <DataTemplate x:Key="defaultCelestialObjectTemplate">
            <Ellipse
                Width="100"
                Height="100"
                Fill="Red" />
        </DataTemplate>
    </UserControl.Resources>-->

    <!--<ContentControl ContentTemplate="{Binding RelativeSource={RelativeSource AncestorType=local:CelestialObject}, Path=CelestialObjectTemplate, FallbackValue={StaticResource defaultCelestialObjectTemplate}}" />-->
    <StackPanel Orientation="Horizontal">

        <Ellipse
            Width="5"
            Height="5"
            Fill="Red" />

        <TextBlock Foreground="LightGreen" Text="{Binding}" />

    </StackPanel>


</UserControl>
